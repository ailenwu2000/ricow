# 规格质量检查清单: bStock 正期望选股策略 bs_rs_rotation(010-bs-rs-rotation)

**目的**: 进入 plan 前校验规格完整性与质量
**创建**: 2026-09-12
**功能**: [spec.md](../spec.md)

## 内容质量

- [x] 无实现细节泄漏(数据来源/已有能力仅列"现状与复用"作前提事实, 不含代码结构方案)
- [x] 聚焦用户价值(负期望方向收敛 + 正期望判定门 + 无前视/成本硬约束)
- [x] 所有强制章节完成(背景与范围 / 用户场景 / 需求 / 关键实体 / 成功标准 / 假设 / 风险)
- [x] 中文产物(宪法原则四)

## 需求完整性

- [x] 无 [NEEDS CLARIFICATION] 遗留(边界均在 Edge Cases 定案: 门缺数据即关 / 首小时缺根不买 / 并列名次升序 / 先卖后买 / 合约池如实披露)
- [x] 需求可测且无歧义(FR-001~013 均含参数、门限或断言)
- [x] 成功标准可测量(SC-001~006 含具体数字: 每 bar 期望>0、净收益−等权>0、夏普/回撤、年换手≤300%、费用<5%/年、重放偏差<1e-6)
- [x] 成功标准技术无关(策略行为与回测结果口径, 非代码结构)
- [x] 验收场景齐全(6 用户故事 × Given/When/Then, 含 P0 数据源铁律)
- [x] 边界情况已识别(8 项, 含 DST 边界与合约池规模)
- [x] 范围清晰(范围外: 实盘部署/调参变绿/引入成交量/池构成/第二交易所/改 shannon_grid)
- [x] 依赖与假设已识别(复用能力清单、回归基线替换、文献不可外推、数据前提)

## 功能就绪

- [x] 所有 FR 有明确验收标准(映射到用户故事验收场景)
- [x] 用户场景覆盖主流程(执行时刻/判定门/稳健性/无前视/成本/数据源边界)
- [x] 满足成功标准定义的可测量结果
- [x] 无实现细节泄漏入规格

## 自查记录

- 上游计划已拍板架构(中周期强度排名 + 波动率目标 + 缓冲带 + 市场门/趋势门), 本规格不重开方案选择
- 按 2026-09-12 代码复核**修正了上游计划的三处失效前提**: ①`bs_momentum` 回归基线已随策略下线失效 → 改为全量测试 + `shannon_grid` 冒烟; ②组合回测 CLI 入口已随下线删除 → 列为"缺, 本变更重建"; ③`scripts/research/` 现为空、`us_klines` 无刷新入口 → 列为数据准备任务
- 判定门 G1–G6 从上游计划原样搬入 SC-001~006, **未放宽**(用户要求: 判定门是唯一验收标准)
- 遗留待用户确认项(已写入 Assumptions, 不阻塞本规格): "横截面选股策略已经去掉"的删除边界 —— 若指"`bs_rs_rotation` 不再做", 本变更应转为止损并改为清理变更

- 本规格**已随终止作废**(见 spec.md 终止记录), 下表检查在终止前完成, 现仅作留档; 不再进入 `/speckit-plan`

**结论**: 规格质量全部通过; 但**变更已终止(2026-09-12 用户判定: 横截面选股策略长期期望值为负)**, 不进入 plan/implement。
