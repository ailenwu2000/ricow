# 015 任务分解

> 状态: **全部完成** (2026-09-13) | 基线: 301 → **304 passed / 0 failed / 11 ignored**(零警告)

- [x] **T001** 新增 `is_noop_fill()`(基于既有 `split_fill`)—— 判定"既不平也不开"
- [x] **T002** `match_pending`: 零动作 → 保持挂单(不计费/不记笔数)
- [x] **T003** `place_order` 即时成交分支: 零动作 → 市价拒单计数 / 限价挂单
- [x] **T004** 单测 3 例: 市价零动作拒单; 限价零动作挂起(并验证仓位出现后能成交平仓); one-way 无仓卖出仍开空
- [x] **T005** 真实数据对照: 修复前 成交 1 / 手续费 0.0482688 / 拒单 0 → 修复后 成交 0 / 手续费 0 / 拒单 1
- [x] **T006** 回归对照: hedge 20d(`2 / 25.1442 / -25.1442 / 0`)、shannon_grid 2d(`4 / 52.5278… / 1.6787… / 0`)修复前后逐位一致
- [x] **T007** 文档同步 + `converge.md`

## 实施记录 (2026-09-13)

- 判定与处置落在**成交那一刻**, 下单入口不动 —— 避免误伤"先挂保护性平仓单、后建仓"的正常用法(D3)。
- 首次实现把限价单也一律拒单, 单测立刻暴露问题(限价单在下单当刻即命中时被拒): 改为按 `order_type` 分流(D2)后通过。
- 真实对照(同窗口): 成交笔数 `1 → 0`、手续费 `0.0482688000000000010047962462 → 0`、
  净盈亏 `-0.0482688000000000010047962462 → 0`、拒单 `0 → 1`。

**留档教训**: 报告里"拒单次数"的注释当时已写着"资金不足/无仓可平" —— 也就是说"无仓可平本该被拒"这件事一直写在 UI 文案里,
但撮合层从未实现。**文案与行为不一致**是最容易被忽略的缺陷信号。
