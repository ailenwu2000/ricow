# 审核报告: Locus AI 助手计划 (019-ai-assistant)

> **被审对象**: 工作稿 `.hermes/plans/2026-09-14_102752-locus-ai-assistant.md`(r3, 435 行);
> **归档说明**: 本文件为审核报告归档副本;审核结论已全部落地到本目录 `spec.md` / `plan.md` / `tasks.md`(三件套为准, 工作稿仅作详细叙述留档)。
> **审核侧重**: `plan-audit` + `plan-review` 清单(任务编号/交叉引用/事实核实/接口存在性/行为变化连带/文档同步/陈旧状态)
> **审核方式**: 只读 —— 逐条拿计划里的断言去对代码、crates.io、官网文档实测; 未运行任何会改动仓库的命令
> **结论**: **可执行(建议修订后进入 SDD)**。🔴 3 项 / 🟡 6 项 / 🟢 2 项, 已全部落地为 r4(见下"修复去向")
> **日期**: 2026-09-14

---

## 一、发现清单(按严重度分级)

| # | 严重度 | 问题 | 证据 | 修复 |
|:--|:--|:--|:--|:--|
| **F1** | 🔴 | **计划状态陈旧**:用户已拍板("其他都同意"+"不花钱做签名"),但计划头部仍写"待用户拍板"、§四标题"需你拍板"、§十"待你回答的问题" —— 执行者会以为还要等拍板或自行选边 | 计划 r3 原文 第3行 / §四标题 / §十标题;用户本轮表态 | 头部改「✅ 已拍板(2026-09-14)」;§四改「已拍板」并给 D 项状态汇总;§十改为「拍板记录(7 项表)」+ 原始提问折叠留档 |
| **F2** | 🔴 | **"改脚本"无路径,与既有"同名拒绝覆盖"硬冲突**:计划把"AI 管理运行中的策略(含调参/改策略)"当作目标,但 `execute_strategy` 同名**拒绝覆盖**(002 FR-005),计划只定义了 `/params`(改参数),没定义"改脚本"怎么落地 → 执行到 Phase 4.5 会卡住或自造覆盖逻辑(可能静默毁掉用户已部署文件) | `crates/locus_engine/src/strategy.rs:118`(同名即 Err);计划 §一 目标与 Phase 4.5 | 新增 **D19**:默认新名部署;另提供受控覆盖 `--replace`(确认块 + 先备份 `<name>.lua.<ts>.bak`);新增 Phase 9.5 任务与验收 |
| **F3** | 🔴 | **AI 生成中文策略名会踩既有缺陷**:订单归属前缀由策略名派生,非 ASCII 字符被统一替换成 `-` → 不同中文名会得到**同一个前缀**,而停机撤单兜底**按前缀判归属**(`is_owned`)→ 可能**误撤同机另一实例的挂单**;且前缀过长时 `inject_prefix` 的 `room=0` 分支会产出超 36 字符订单号 → 交易所拒单。AI 面向中文用户,**几乎必然生成中文名** → 高概率触发 | `crates/locus_strategy/src/align.rs:134-143`(`ownership_prefix` 逐字符 `is_ascii_alphanumeric() else '-'`)、`:145-148`(`is_owned` 仅 `starts_with`)、`:150-168`(`inject_prefix` 超长分支)、`MAX_CLIENT_ORDER_ID_LEN = 36` | 新增 **D18**:策略名仅 `[A-Za-z0-9_-]`、长度 ≤24、同前缀冲突拒绝;`create` 阶段报错并给解法;提示词/agent kit 手册同约束;新增 SC-13 与 Phase 9.1 |
| **F4** | 🟡 | **L1 语义不准**:计划写 L1"AI 可直接做(无真实资金, preview 不落盘)",读起来像"零副作用"。实际 **Dry Run 首次启动会写策略 TOML 的 `dry_run_started_at`**(= 开始计时, 直接决定实盘时长门禁),并真起子进程、写日志与成交表 | `crates/locus_cli/src/commands/run.rs:142-147`(首次启动写起点并打印"已记录 Dry Run 起点…(实盘时长门禁依据)") | D3 中 L1 改为「虚拟**且有副作用**」并写清三件事(计时/子进程/日志);新增 Phase 9.6「启动前告知会开始计时」+ 验收 |
| **F5** | 🟡 | **`previews` 表无清理,AI 迭代会放大堆积**:`previews` 表只标状态不删行(注释:"过期记录也需要能清理(标记 rejected 为终态)"),AI 一轮迭代可能连打 5-10 次 preview,每行含完整 Lua + 报告 payload → 缓慢膨胀 | `crates/locus_strategy/src/db.rs:125`(建表)/`:485`(插入);`crates/locus_engine/src/confirm.rs:11`(TTL 15min)/`:81-95`(reject 只置状态) | 新增 Phase 9.2:清终态/过期行(不新增表)+ SC-14 行数对照 |
| **F6** | 🟡 | **工具输出无上限 → 上下文与成本失控**:计划只约束了提示词约 20KB,没有约束工具返回值(`fills`/`logs_tail`/回测报告/`scan`)的长度,也没有长会话裁剪策略 → 小上下文模型会直接爆,成本不可控(与 R3 自相矛盾) | 计划 r3 §3.4-3 / R3 / SC-7;`instances.rs:36,388`(`--limit` 存在但无服务端上限约定) | 新增 Phase 9.3:各工具输出上限(`fills`≤50 行、`logs_tail`≤200 行、报告单块、`scan`≤20 条)+ 截断标记;R18 |
| **F7** | 🟡 | **SC-6 与 `logs_tail` 存在张力**:SC-6 写"外发不含 key/secret/env",但 `logs_tail` 返回的是**自由文本**(可能含余额/持仓/命令/报错原文)。虽有实测支持(代码中无打印密钥的语句),但 SC 的写法会被读成"日志也保证不含敏感信息" | 实测 `grep` 全仓 `println!/tracing!` 中无 secret/api_key 字样(见"已核验无问题项");但日志内容本身不受控 | SC-6 措辞精确化(不含 key/secret/env + 账户数据仅发到用户自配端点 + 日志做敏感模式过滤 + 外发清单明示"日志原文可能外发");Phase 9.4 |
| **F8** | 🟡 | **rig 版本约束写法不精确**:计划多处写 `rig = "=0.42"` / "锁 `=0.42.x`"。实测 `cargo add rig@=0.42` 语法被接受,但语义含混,且计划没提"Cargo.lock 入库"才是可复现的真正保证 | `cargo add rig@=0.42 --dry-run`(接受);`cargo add rig@0.42`(caret) | 统一改 `rig = "0.42"`(caret 语义 `>=0.42.0 <0.43.0` = 锁 0.42 线;要绝对锁死写 `=0.42.0`),并写明 **Cargo.lock 入库**(仓库已跟踪) |
| **F9** | 🟡 | **文档同步清单漏点名 `product.md` D12 行**:计划正文承认"这改动了 D12",但 Phase 6.1 的文档清单没有点名 D12(以及 §四.4、§五 形态图里的 MCP 表述),执行者可能只改"总述"不改决策行 | `specs/product.md:111`(D12「MCP / Telegram 暂不做」)/ `:35`(§四.4 竞品 MCP 观察期)/ `:49`(§五 入口后置) | Phase 9.7 明确点名 D12 行 + 同步 §四.4/§五,验收用 `grep`(不能再出现「MCP 暂不做」) |
| **F10** | 🟢 | **任务粒度是"方案级"**:8 个 Phase / 44 行任务,不是 `plan` skill 要求的"2-5 分钟一步"。直接照做会有大量临场设计空间 | 计划 §六 | §六 顶部保留 Phase 结构(便于按阶段交付)但明确:执行时按 `plan-execution` 展开逐项可验证清单(计划里已如此标注) |
| **F11** | 🟢 | **三档入口与 D 编号映射缺失**:§3.5 三档(agent kit / MCP / 内置 AI)与 D12/D13/D14/D16 的对应关系没写,读者需自行推断 | 计划 §3.5 与 §四 | §3.5 设计要点补第 6 条映射 |

---

## 二、已核验"没问题"的项(避免下一轮重复劳动)

| 检查项 | 方法 | 结果 |
|:--|:--|:--|
| 引用到的 12 个既有 API 是否真实存在 | `grep` 逐个查 `crates/*/src` | ✅ 全部存在(`create_strategy`/`execute_strategy`/`approve`/`create_preview`/`backtest_and_preview`/`dry_run_gate`/`risk_gate`/`check_clock_skew`/`record_dry_run_start`/`deployed_strategy_names`/`load_strategy_toml`/`print_backtest_report`) |
| 计划引用的既有文件存在 / 计划新建的文件当前不应存在 | 逐个 `test -e` | ✅ 8 个既有文件 OK;6 个新建文件均不存在(无"以为要新建其实已有"的错位) |
| 顶层章节顺序与编号连续性 | `grep '^## '`、`grep '^### Phase'` | ✅ 一~十 连续;Phase 1~8 连续(新增 Phase 9 后仍连续在 §七 之前) |
| D 编号有无悬空 | `grep -oE 'D1[0-7]|D[1-9]'` | ✅ 仅 D1–D19,无引用不存在的编号 |
| 代码块围栏配对 | `grep -c '```'` | ✅ 2(偶数, `.cmd` 片段一对) |
| 是否可能把密钥打进日志 | `grep` 全仓日志宏 × secret/api_key | ✅ 无(0 命中);错误文案只给"怎么配置",不回显凭据 |
| 运行时是否依赖仓库内的非嵌入式文件(分发缺口) | `grep 'presets\|examples/'` in `crates/*/src` | ✅ 不读 → 下载单个二进制不会缺文件(内置脚本是 `include_str!` 编译期嵌入) |
| rig 是否默认外发遥测 | rig-core 依赖清单(34 个 normal deps) | ✅ 无上报/分析类依赖;OTel(GenAI 语义约定)为可选集成,不启用不发 → 与宪法原则一"无遥测"一致 |
| 测试基线数字 | 与 `specs/roadmap.md` 对照 | ✅ 308 passed / 11 ignored 一致 |
| 仓库分发现状(计划前提) | `ls -a`、`ls .github`、`grep README` | ✅ 实测确认:无 `.github/`、无安装脚本、README 无安装章节(前提成立) |

---

## 三、修复去向(r4 已落地)

| 发现 | 落地位置 |
|:--|:--|
| F1 | 文档头「✅ 已拍板(2026-09-14)」+ r4 修订说明;§四标题与状态行;§十改为拍板记录(7 项)+ 原始提问折叠留档 |
| F2 | 新增 D19 + Phase 9.5 + 改动清单(受控覆盖与备份) |
| F3 | 新增 D18 + Phase 9.1 + SC-13 + R16 + 改动清单(`strategy.rs` 名校验) |
| F4 | D3 中 L1 段改写 + Phase 9.6 + Dry Run 计时告知 |
| F5 | Phase 9.2 + SC-14 + R17 + 改动清单(`confirm.rs`) |
| F6 | Phase 9.3 + SC-14 + R18 + 改动清单(`ai/tools.rs`) |
| F7 | SC-6 措辞精确化 + Phase 9.4 |
| F8 | 改动清单与 Phase 1.1、R4 统一为 `rig = "0.42"` + Cargo.lock 入库 |
| F9 | Phase 9.7 点名 `product.md` D12 行 + §四.4/§五 同步 |
| F10 | §六 顶部注明执行前按 `plan-execution` 展开 |
| F11 | §3.5 设计要点第 6 条(三档 ↔ D 编号映射) |

---

## 四、仍未解决 / 需要用户点头的两项(审核新增)

1. **D18 策略名规范**(🔴 F3 的结构性处置)—— 属"既有缺陷 + 本变更放大",建议照 D18 执行;若用户希望"允许中文名"则需另设前缀唯一化机制(更复杂,不推荐)。
2. **D19 改脚本路径**(🔴 F2 的结构性处置)—— 建议"默认新名 + 受控 `--replace` 带备份";若用户希望"直接覆盖",则必须保留备份并在确认块里显示将覆盖的旧文件摘要。

> 其余 🟡/🟢 项属实现级修正,已在 r4 计划内落地,无需单独拍板。

---

## 五、复验命令(审核后重跑, 用于确认修订没有引入新问题)

```bash
cd /mnt/d/mywork/locus
F=.hermes/plans/2026-09-14_102752-locus-ai-assistant.md
grep -n '^## ' $F                       # 章节顺序应为一~十 连续
grep -n '^### Phase' $F                 # Phase 1~9 连续
grep -c '```' $F                        # 应为偶数
grep -oE 'D1[0-9]|D[1-9]' $F | sort -Vu # 编号应连续无悬空
grep -n '待用户拍板\|需你拍板\|待你回答' $F   # 应为空(状态已更新)
grep -n 'rig = "0.42"' $F | wc -l       # 版本约束写法统一
```
