# 018 收敛评估 (converge)

**日期**: 2026-09-13 | **范围**: 首次使用风险确认 | **结论**: ✅ 全部完成

## 一、验收标准逐项实测

| # | 判据 | 实测 | 结论 |
|:--|:--|:--|:--|
| SC-001 | 全量测试零回归 | **308 passed / 0 failed / 11 ignored**(306 → 308), 零警告; `cargo build` 通过 | ✅ |
| SC-002 | 纯逻辑单测 | 首次拒绝(消息含披露要点 / `--accept-risk` / README 指引); 带开关 → `JustAcked`; 已确认 → `Proceed` | ✅ |
| SC-003 | 真实三步(全新数据目录 + demo) | ① 拒绝且 `risk_ack.json` **不存在**; ② `--accept-risk` → `已记录风险确认: /tmp/locus-risk-018/risk_ack.json`, 文件含 `version/disclosure/acked_at`; ③ 不带开关再跑 → 不再被风险闸拦, `时钟预检通过 +513ms` 并正常启动 | ✅ |
| SC-004 | 文档同步 | `architecture.md` §七 + §九 基线 / `product.md` §十 标注落地 / `roadmap.md` 018 行 / P4 runbook 首次实盘步骤 | ✅ |

## 二、计划外发现

1. 给 clap 结构体加字段会打断**测试内的字面构造**(`ctrl.rs` 一处 `StartArgs{..}`)→ 已修; 属小坑, 已记入 tasks。
2. 验证③顺带暴露了一个正面事实: 判定顺序(风险 → 时长 → 时钟)在真实链路上确实是从早到晚生效的, 未确认时**零交易所往返**。

## 三、收敛遗留

- 披露内容目前是四条要点(与 README 一致)。若日后新增风险类目(如合约杠杆), 递增 `RISK_ACK_VERSION` 即可触发重新确认, 无需改逻辑。
- 未做多语言披露(README 有中英双份, 确认消息为中文) —— 与当前 CLI 中文输出一致。

## 四、留档教训

- **文档里的"承诺"要能对上代码**: 本次核对 `product.md` §十 时发现"README 免责声明"已做、"首次使用风险确认"没做 —— 同一行里两条承诺, 一条落地一条空转, 这类**半落地**最容易被漏掉。
