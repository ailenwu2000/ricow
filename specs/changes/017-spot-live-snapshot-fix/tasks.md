# 017 任务分解

> 状态: **全部完成** (2026-09-13) | 基线: **306 passed / 0 failed / 11 ignored**(零警告)

- [x] **T001** 定位 A: 012 起清理用 `get_positions_directional`, 现货实现恒空 → `plan_cleanup` 判定"无持仓"
- [x] **T002** 定位 B: 成交后只刷持仓不刷现金 → 策略以为"有钱没币/有币没钱" → 几何级数卖光(算术逐笔吻合)
- [x] **T003** 定位 C: 成交回写异步 + 无下单后对齐 → 窗口内重复下单(实测 1.4s 内 3 次同单)
- [x] **T004** 修复 A: 清理与残留复查的持仓源按市场分支(现货 `spot_position_of`)
- [x] **T005** 修复 B: `refresh_positions` 现货分支补刷 base+quote 余额(失败只 warn)
- [x] **T006** 修复 C: 实盘下单批次 `any_filled` → 立即 `refresh_positions`
- [x] **T007** 真实复验(demo): 修复前 `提交订单=216 / 拒单=209 / 成交=8`(持仓清空) → 修复后 `提交订单=1 / 拒单=0 / 成交=2 / 平仓单=shannon-demo-c3056810 / 残留=0.000058`
- [x] **T008** 交易所侧核对: `ETH 0.000058`(尘埃) / `openOrders=0` / 成交"买 1.008 → 卖 1.007"一轮往返
- [x] **T009** 文档同步 + `converge.md`

## 实施记录 (2026-09-13)

**时序证据(修复前那一轮)**: 用户流通知逐条留档, 几何级数卖单清晰可见
`Sell 1.0079 → 0.5039 → 0.252 → 0.126 → 0.063 → 0.0315 → 0.016`, 同价 `2475.56`, 合计约等于全部持仓 2.016 ETH。

**为什么这些缺陷没被单测/011-012 抓到**:
- 单测覆盖纯逻辑(`plan_cleanup` 等), 而缺陷在**调用方的数据源选择**与**上下文刷新时序**上;
- 011 现货实盘验证时走的是引擎侧 `spot_position_of`(能平仓), 012 把清理改成 directional 后**只在合约侧做了实盘验证** ——
  现货这条路的回归因此无人覆盖;
- 现货"只刷持仓不刷现金"在 Dry Run 下不会暴露(虚拟撮合同步更新两侧)。

**教训**: 跨市场共用的"持仓/余额"取数点, 改动后必须**逐市场**回归实跑, 不能以"改了合约、现货走默认实现"为理由跳过。
