# 安全策略(Security Policy)

## 报告漏洞

**请不要在公开 issue 里提交漏洞细节或任何凭据。**

优先渠道(任一):

1. 仓库 **Security** 页 → **Report a vulnerability**(GitHub 私有漏洞报告,只有维护者可见);
2. 若上述入口不可用,在 GitHub 上联系 [@ailenwu2000](https://github.com/ailenwu2000),先只说明"我要报告安全问题",细节等私下通道建立后再给。

请在报告中包含:受影响版本(commit 或 tag)、复现步骤、影响面(资金安全 / 凭据泄露 / 拒绝服务)、你的建议。我们会在确认后给出修复与披露时间线。

## 支持版本

| 版本 | 支持 |
|---|---|
| 最新 release(`v0.x` 线) | ✅ 接受报告并修复 |
| 更早版本 | ❌ 请先升级到最新 release 再验证 |

## 威胁模型(本项目最需要防的几类问题)

1. **凭据泄露**:`ricow.toml` 里明文存放币安与 LLM 密钥。任何导致密钥被打印到日志、上报到远端、写进仓库或随 `agent-kit`/AI 助手输出外泄的路径,都按安全问题处理。
2. **误入实盘 / 越权下单**:实盘需要策略声明 `live_enabled = true` + `--live --accept-risk` + 首次风险确认 + Dry Run 时长门禁。任何绕过这些门(`确认实盘 <名字>` 可被非交互输入接受、门禁可被配置静默降级、demo 与主网凭据互相回落)都是高危缺陷。
   > **两套确认渠道(023)**: 写操作 = 会改磁盘或改运行态的动作, 共 13 类(落盘 / 覆盖 / 改参数 / 删除 / 试跑与测试网启停 / 实盘全部动作), 全部须一次确认; 只读动作(回测 / 查询 / 预览)不需确认。确认词**分渠道** —— **对话**渠道(`ricow` REPL / `ricow ai`)用**当前语言的单个口语词**(`确认`/`确定`/`同意` · `confirm`/`confirmed`, 只认当前语言, 刻意不含 `y`/`yes`/`ok`/空); **终端**渠道(`ricow approve` / `ricow start --live` / `ricow stop --live`)保持**逐字长短语**(`确认部署 <名>` 等)**一行不改**。无论哪条渠道, 执行者始终是**宿主进程**, LLM 的写实工具面**仍然为空**(结构性边界不变)。
3. **提现与权限**:产品只应使用"交易"权限的 API Key。任何引导用户开启提现权限的文档或代码都视为缺陷。
4. **远程代码执行 / 数据外泄**:Lua 沙箱(无 os/io/require/loadstring/pcall、指令预算、内存上限)与"本机控制通道(token)"是边界;沙箱逃逸、控制通道未鉴权/被本机其他进程劫持、AI 助手的工具面越界(把写实操作暴露成可调用工具)都属于安全问题。
5. **依赖供应链**:依赖应保持可审计(锁文件入库、CI 只跑标准 GitHub runner)。发现恶意或被投毒的依赖请按上面渠道报告。

## 我们的承诺

- 不引入遥测、不自动上传任何本地数据(宪法原则一"完全本地化")。
- 密钥只从本机文件/环境变量读取,不代写、不外发。
- 修复类安全问题会尽可能附带回归测试;涉及真实资金的路径改动会在 demo 测试网实测后再合并。
