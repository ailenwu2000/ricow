## 这个 PR 做了什么

<!-- 一句话说明;关联 issue 用 "Closes #123" -->

## 自检清单(逐条确认)

- [ ] CI 全绿(lint + test)
- [ ] 不含任何密钥/凭据/个人数据(ricow.toml、ricow.db* 已忽略;没有用 -f 强行加入)
- [ ] 改动触及交易路径(下单/撤单/成交/持仓/风控) → 已在 demo 测试网真实调用验证, 并在下方写明实测结论
- [ ] 改动影响系统级行为 → 已同步更新 specs/ 里受影响的章节与测试基线数字
- [ ] 新增/变更依赖 → 已在描述里说明理由(宪法原则五"少而精")
- [ ] 改动过的文件已按 rustfmt.toml 格式化(全仓对齐是待办, 见 CONTRIBUTING 第五节)

## 测试与验证

<!-- 贴出实际执行的命令与输出摘要;不要写"应该可以"。真实数据/真实调用优先, 禁止 mock 交易所替身 -->

```
cargo test --workspace
```

## 交易路径实测结论(如适用)

<!-- 例如: demo 现货挂单→停机撤单兜底→交易所零残留;或写"未实测, 原因: ..."(诚实优先于绿色) -->

## 其他说明 / 截图
